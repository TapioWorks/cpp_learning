# Calculator to calculate the are of a rectangle

print("This calculator solves a area of a rectangle\n")

length = float(input("Type the length of the rectangle in cm: "))
width = float(input("Type the width of the rectangle in cm: "))

area = length * width

print(f"the answer is: {area}cm²")